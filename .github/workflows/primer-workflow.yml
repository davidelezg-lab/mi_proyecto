name: Uso de variables limpio

on:
  push:
    branches:
      - prueba

jobs:
  ejemplo:
    runs-on: ubuntu-latest

    env:
      ENTORNO: desarrollo

    steps:
      - name: Mostrar variable
        run: |
          echo "Entorno actual: $ENTORNO"
