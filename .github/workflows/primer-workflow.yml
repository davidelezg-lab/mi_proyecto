name: Uso de variables

on:
  push:
    branches:
      - prueba

jobs:
  variables:
    runs-on: ubuntu-latest

    env:
      ENTORNO: desarrollo

    steps:
      - name: Mostrar variable
        run: |
          echo "Entorno actual: $ENTORNO"

