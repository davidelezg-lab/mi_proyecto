name: Ejecutar comandos

on:
  push:
    branches:
      - developer

jobs:
  comandos:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Ver archivos
        run: ls -la

      - name: Mostrar fecha
        run: date
