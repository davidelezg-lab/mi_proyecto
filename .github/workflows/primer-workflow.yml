name: Matriz de versiones

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        version: [1, 2, 3]

    steps:
      - name: Test versión
        run: echo "Versión ${{ matrix.version }}"
