name: Uso de variables limpio

on:
  push:
    branches:
      - main

jobs:
  ejemplo:
    runs-on: ubuntu-latest

    env:
      ENTORNO: desarrollo

    steps:
      - name: Mostrar variable
        run: |
          echo Entorno actual: $ENTORNO
