name: Manual con input

on:
  workflow_dispatch:
    inputs:
      nombre:
        description: 'Tu nombre'
        required: true

jobs:
  saludo:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Hola ${{ github.event.inputs.nombre }}"
