name: Uso de secretos

on:
  push:
    branches:
      - main

jobs:
  secreto:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar secreto
        run: echo "Token: ${{ secrets.MI_TOKEN }}"
